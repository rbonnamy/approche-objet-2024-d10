package math;

// On importe le strict nécessaire ! Pensez à la réorganisation des imports.
import static java.lang.Math.PI;
import static java.lang.Math.pow;

public class TestMath {
    public static void main(String[] args) {
        double rayon = 2.5;
        double valeur = PI * pow(rayon, 2.0);
    }
}
